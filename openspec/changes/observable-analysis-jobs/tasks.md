# Tasks — observable-analysis-jobs

Total: 78 items.

## 1. Dominio y enums

- [x] 1.1 Crear paquete `backend/src/main/java/dev/diegobarrioh/tokenmeter/domain/job/` con `package-info.java` que documente el bounded context del job (sin dependencias de Spring ni JPA).
- [x] 1.2 Crear `domain/job/AnalysisJobId.java` como `record AnalysisJobId(UUID value)` con factory `random()` y validación `Objects.requireNonNull` en el canonical constructor.
- [x] 1.3 Crear `domain/job/AnalysisJobStatus.java` con valores `QUEUED, RUNNING, SUCCESS, FAILED` y método `boolean isTerminal()` (true para `SUCCESS` y `FAILED`).
- [x] 1.4 Crear `domain/job/AnalysisJobPhase.java` con `QUEUED, CHECKING_CACHE, CLONING_REPOSITORY, SCANNING_FILES, FILTERING_FILES, COUNTING_TOKENS, CALCULATING_COSTS, SAVING_REPORT, COMPLETED, FAILED`, `boolean isTerminal()`, `int forwardRank()` y `boolean canTransitionTo(AnalysisJobPhase next)` aplicando las reglas del spec (forward-only en happy-path, salto libre a `FAILED`, terminal inmutable).
- [x] 1.5 Crear `domain/job/AnalysisJobErrorCode.java` con `CLONE_TIMEOUT, REPOSITORY_TOO_LARGE, INVALID_URL, ANALYSIS_FAILED, JOB_INTERRUPTED, RATE_LIMITED` y método helper `static AnalysisJobErrorCode fromIntakeCode(RepositoryIntakeErrorCode)` para mapear desde la excepción existente.
- [x] 1.6 Crear `domain/job/AnalysisJobMetrics.java` como `record` con `Long filesDiscovered, filesProcessed, filesSkipped, tokensCounted; Integer contextWindows, pricingModelsProcessed` (todos nullable) y canonical constructor que rechaza valores negativos.
- [x] 1.7 Crear `domain/job/AnalysisJobSnapshot.java` (`record` con `AnalysisJobId id`, `AnalysisJobStatus status`, `AnalysisJobPhase phase`, `int progressPercent`, `String message`, `UUID analysisId`, `AnalysisJobErrorCode errorCode`, `String errorMessage`, `AnalysisJobMetrics metrics`, `Instant createdAt, startedAt, updatedAt, completedAt`) sin lógica de negocio (solo holder para responses internas).
- [x] 1.8 Añadir test `backend/src/test/java/dev/diegobarrioh/tokenmeter/domain/job/AnalysisJobPhaseTest.java` que cubra: orden forward-only del happy-path, salto a `FAILED` desde cualquier no-terminal, rechazo de retrocesos, terminal-inmutable (`COMPLETED` y `FAILED` no transicionan).
- [x] 1.9 Añadir test `backend/src/test/java/dev/diegobarrioh/tokenmeter/domain/job/AnalysisJobStatusTest.java` que cubra `isTerminal()` y la propiedad "terminal → no transition" (helper de test).
- [x] 1.10 Añadir test `backend/src/test/java/dev/diegobarrioh/tokenmeter/domain/job/AnalysisJobMetricsTest.java` que valide rechazo de métricas negativas (`IllegalArgumentException`) y aceptación de `null` por campo.

## 2. Persistencia

- [ ] 2.1 Crear migración `backend/src/main/resources/db/migration/V6__analysis_job.sql` con la DDL del diseño: tabla `analysis_job`, FK opcional a `analysis(id)` (`ON DELETE SET NULL`), `CHECK` para status/phase/progress range, invariante `progress<=99 OR (status='SUCCESS' AND analysis_id IS NOT NULL)`, `CHECK` files (`files_processed <= files_discovered`), `CHECK` terminal↔`completed_at`, `CHECK` failed payload (`error_code` y `error_message` no nulos cuando `status='FAILED'`), índices `idx_analysis_job_status`, `idx_analysis_job_completed_at`, `idx_analysis_job_created_at`.
- [ ] 2.2 Crear `infrastructure/persistence/analysis/jobs/AnalysisJobEntity.java` (`@Entity @Table(name="analysis_job")`) con los campos del diseño, `@Enumerated(STRING)` en `status`/`phase`, `@PrePersist` que fija `createdAt`/`updatedAt`, `@PreUpdate` que refresca `updatedAt`. Sin Lombok.
- [ ] 2.3 Crear `infrastructure/persistence/analysis/jobs/AnalysisJobJpaRepository.java` extendiendo `JpaRepository<AnalysisJobEntity, UUID>` con derived queries `List<AnalysisJobEntity> findByStatusIn(Collection<AnalysisJobStatus> statuses)` y `List<AnalysisJobEntity> findByStatusAndCompletedAtBefore(AnalysisJobStatus status, Instant cutoff)`.
- [ ] 2.4 Crear `application/analyzer/AnalysisJobRepository.java` (puerto en application) con métodos `save(AnalysisJobSnapshot initialQueued)`, `markStarted(AnalysisJobId id, Instant when)`, `updatePhase(AnalysisJobId id, AnalysisJobPhase phase, int progressPercent, String message)`, `updateMetrics(AnalysisJobId id, AnalysisJobMetrics metrics)`, `markSuccess(AnalysisJobId id, UUID analysisId, AnalysisJobMetrics finalMetrics, Instant when)`, `markFailed(AnalysisJobId id, AnalysisJobErrorCode code, String message, Instant when)`, `markInterrupted(AnalysisJobId id, Instant when)`, `Optional<AnalysisJobSnapshot> findById(AnalysisJobId id)`, `List<AnalysisJobSnapshot> findNonTerminal()`, `int deleteCompletedBefore(AnalysisJobStatus status, Instant cutoff)`, `void deleteById(UUID id)`.
- [ ] 2.5 Crear `infrastructure/persistence/analysis/jobs/JpaAnalysisJobRepository.java` (`@Component`) que implementa `AnalysisJobRepository` delegando en el `AnalysisJobJpaRepository` y mapeando `AnalysisJobEntity ↔ AnalysisJobSnapshot` (mapper privado en la misma clase, sin librería externa).
- [ ] 2.6 Añadir `@DataJpaTest` en `backend/src/test/java/dev/diegobarrioh/tokenmeter/infrastructure/persistence/analysis/jobs/JpaAnalysisJobRepositoryTest.java`: (a) insertar fila mínima `QUEUED`, (b) verificar que H2 rechaza `progress_percent=100` sin `analysisId` (constraint violation), (c) `findByStatusAndCompletedAtBefore` filtra por antigüedad, (d) `findByStatusIn(QUEUED, RUNNING)` devuelve no-terminales.
- [ ] 2.7 Verificar que la suite `MigrationFlywayTest` (o equivalente) sigue verde sobre H2 + Postgres con V6 aplicada; si no existe, añadir un test `@FlywayTest` que asegure migración limpia desde V1 hasta V6 (PostgreSQL container con Testcontainers no es obligatorio: bastar con H2 + `H2Dialect.MYSQL`/`PostgreSQL`).

## 3. Capa application

- [ ] 3.1 Modificar `application/analyzer/AnalyzeThrottleProperties.java` para añadir `int queueCapacity` (default 32), `Retention retention(Duration success default P7D, Duration failed default P30D, String cron default "0 30 3 * * *")`. Mantener compatibilidad: canonical constructor con defaults para no romper los tests existentes.
- [ ] 3.2 Crear `application/analyzer/AnalysisJobProgressEmitter.java` (puerto) con `transition(AnalysisJobId, AnalysisJobPhase, int progressPercent, String message)`, `updateMetrics(AnalysisJobId, AnalysisJobMetrics)`, `success(AnalysisJobId, UUID analysisId, AnalysisJobMetrics finalMetrics)`, `fail(AnalysisJobId, AnalysisJobErrorCode, String message)`.
- [ ] 3.3 Crear `infrastructure/persistence/analysis/jobs/JpaAnalysisJobProgressEmitter.java` (`@Component` implementa el puerto) con `@Transactional(propagation = REQUIRES_NEW)` en cada método. Aplica el clamp `progressPercent = max(0, min(99, value))` salvo en `success` (único punto que pone 100). `fail` es no-op si el job ya está terminal (idempotente). Añade logging estructurado (`jobId`, `phase`, `progressPercent`) y limpia el MDC en `finally`.
- [ ] 3.4 Añadir helper `application/analyzer/MdcScope.java` (try-with-resources `AutoCloseable`) que encapsula `MDC.put("jobId", id)` / `MDC.remove("jobId")` para uso en emitter y execution service.
- [ ] 3.5 Crear `application/analyzer/AnalysisJobQueryService.java` (`@Service`, read-only) con `Optional<AnalysisJobSnapshot> findById(AnalysisJobId id)` delegando al repositorio.
- [ ] 3.6 Crear `application/analyzer/AnalysisJobSubmissionService.java` (`@Service`): valida la URL con `GitHubRepositoryUrl.parse` (lanza `RepositoryIntakeException(INVALID_URL)` si falla), persiste `QUEUED` vía `AnalysisJobRepository.save`, llama `executor.execute(() -> executionService.runJob(jobId))`. Si `TaskRejectedException`/`RejectedExecutionException`: borra el job recién persistido y lanza `RepositoryIntakeException(RATE_LIMITED)`. Devuelve `AnalysisJobSnapshot` resultante (status, jobId).
- [ ] 3.7 Crear `application/analyzer/AnalysisJobExecutionService.java` (`@Service`) con `@Async("analysisJobExecutor")` `runJob(AnalysisJobId id)`. Reubica la pipeline actual (`cloner`, `sizeCalculator`, `fileScanner`, `tokenizationService`, `costEstimationService`, `persistenceService`) emitiendo `emitter.transition(...)` al entrar/salir de cada fase y `emitter.updateMetrics(...)` cuando proceda. Throttle de emisiones en `COUNTING_TOKENS` con `lastEmitNanos` local cada ~250 ms. `try/catch`: `RepositoryIntakeException` → `emitter.fail(code, msg)`; `Throwable` → `emitter.fail(ANALYSIS_FAILED, msg)`. `finally`: `deleteRecursively(tempDir)` (preservar la lógica actual).
- [ ] 3.8 Refactor en `application/analyzer/RepositoryAnalysisService.java`: eliminar `analyze(String url)` síncrono. Mantener solo `findById(UUID id)` y los colaboradores que sean reutilizados desde fuera (si los hay). Actualizar Javadoc para indicar que la orquestación vive en `AnalysisJobExecutionService`.
- [ ] 3.9 Eliminar la dependencia de `AnalysisConcurrencyGuard` desde `RepositoryAnalysisService` y `AnalysisJobSubmissionService`. El semáforo se reemplaza por la cola del executor (paridad: rejection → 429). Mantener la clase si tiene tests/utilidades, marcar `@Deprecated` y borrar al cerrar el ticket.
- [ ] 3.10 Crear `application/analyzer/AnalysisJobReaper.java` (`@Component implements ApplicationRunner`): en `run(ApplicationArguments)` llama `repository.findNonTerminal()` y por cada uno ejecuta `repository.markInterrupted(id, Instant.now())` (deja `status=FAILED, phase=FAILED, errorCode=JOB_INTERRUPTED, errorMessage="Analysis interrupted by service restart"`). Loguear `WARN` con cuenta total.
- [ ] 3.11 Crear `application/analyzer/AnalysisJobRetentionScheduler.java` (`@Component`): `@Scheduled(cron = "${tokenmeter.analyze-throttle.retention.cron:0 30 3 * * *}")` que ejecute `repository.deleteCompletedBefore(SUCCESS, now - retention.success)` y `deleteCompletedBefore(FAILED, now - retention.failed)`. Inyectar `Clock` para poder testar con tiempo simulado.
- [ ] 3.12 Test unit `AnalysisJobSubmissionServiceTest` (Mockito, sin Spring): URL inválida → `RepositoryIntakeException(INVALID_URL)` sin tocar repo ni executor; executor saturado → repo.save+deleteById+`RepositoryIntakeException(RATE_LIMITED)`; happy-path → repo.save + executor.execute con runnable que invoca `runJob(jobId)`.
- [ ] 3.13 Test unit `AnalysisJobExecutionServiceTest` (Mockito): cubre clone-OK → SUCCESS con `analysisId` y `progress=100`; `RepositoryIntakeException(CLONE_TIMEOUT)` → `emitter.fail(CLONE_TIMEOUT, ...)`; excepción genérica → `emitter.fail(ANALYSIS_FAILED, ...)`; verifica orden de fases emitidas y que `tempDir` se borra en `finally`.
- [ ] 3.14 Test unit `AnalysisJobReaperTest` (Mockito): job `RUNNING` heredado → `repository.markInterrupted` invocado con el id correcto; lista vacía → no-op.
- [ ] 3.15 Test unit `AnalysisJobRetentionSchedulerTest` (Mockito + `Clock.fixed`): verifica que llama `deleteCompletedBefore(SUCCESS, fixedNow - 7d)` y `deleteCompletedBefore(FAILED, fixedNow - 30d)` con los defaults; overrides de `Duration` se respetan.
- [ ] 3.16 Test unit `JpaAnalysisJobProgressEmitterTest` (`@DataJpaTest`): emitir `transition(..., progress=100, ...)` en una fase no-terminal y verificar que la BD acaba con `progress_percent=99` (clamp); `success` con `analysisId` no nulo persiste `progress=100, status=SUCCESS, phase=COMPLETED, completed_at != null`; `fail` sobre job terminal es no-op.
- [ ] 3.17 Migrar `application/analyzer/RepositoryAnalysisServiceTest.java` (existente): renombrar a `AnalysisJobExecutionServiceTest` si todavía no se ha creado en 3.13 (consolidar en un solo archivo). Reemplazar `service.analyze(url)` por `service.runJob(jobId)` previa creación del job `QUEUED` en un fake del `AnalysisJobRepository`. Aserciones sobre snapshot final, no sobre el retorno del método.

## 4. Executor y scheduling

- [ ] 4.1 Crear `infrastructure/config/AsyncExecutionConfig.java` (`@Configuration @EnableAsync @EnableScheduling`) con `@Bean("analysisJobExecutor") ThreadPoolTaskExecutor` configurado a partir de `AnalyzeThrottleProperties` (`corePoolSize = maxPoolSize = maxConcurrent`, `queueCapacity`, `threadNamePrefix="tm-job-"`, `RejectedExecutionHandler = AbortPolicy`, `waitForTasksToCompleteOnShutdown=true`, `awaitTerminationSeconds=30`).
- [ ] 4.2 Verificar que `PricingSchedulingConfig` y `AsyncExecutionConfig` no chocan al activar `@EnableScheduling` por duplicado; documentar en comentario.
- [ ] 4.3 Actualizar `backend/src/main/resources/application.yml` con `tokenmeter.analyze-throttle.queue-capacity`, `retention.success`, `retention.failed`, `retention.cron` y sus env vars (`TOKENMETER_ANALYZE_QUEUE_CAPACITY`, `TOKENMETER_JOB_RETENTION_SUCCESS`, `TOKENMETER_JOB_RETENTION_FAILED`, `TOKENMETER_JOB_RETENTION_CRON`).
- [ ] 4.4 Crear `infrastructure/config/AsyncExecutionConfigTest.java` (`@SpringBootTest` slice o `@SpringJUnitConfig`): el bean `analysisJobExecutor` se publica, `corePoolSize=maxConcurrent`, `queueCapacity` viene de las properties.
- [ ] 4.5 Test integración `AnalysisJobReaperIntegrationTest` (`@SpringBootTest`): pre-insertar fila `status=RUNNING` directamente con `JdbcTemplate`, arrancar contexto, asegurar que tras el `ApplicationRunner` la fila está en `FAILED/JOB_INTERRUPTED` con `completed_at != null`.
- [ ] 4.6 Test integración `AnalysisJobRetentionSchedulerIntegrationTest` (`@SpringBootTest` con `Clock` bean override): pre-insertar dos jobs `SUCCESS` (uno reciente, uno > 7d) y dos `FAILED` (uno reciente, uno > 30d); invocar manualmente el método del scheduler; verificar que solo los antiguos se borran.

## 5. Capa web

- [ ] 5.1 Crear `infrastructure/web/analyzer/AnalysisJobAcceptedResponse.java` (`record AnalysisJobAcceptedResponse(String jobId, AnalysisJobStatus status, String statusUrl, UUID analysisId)`).
- [ ] 5.2 Crear `infrastructure/web/analyzer/AnalysisJobStatusResponse.java` (`record` con `jobId`, `status`, `phase`, `phaseLabel`, `progressPercent`, `message`, `analysisId`, `error` (sub-record `JobErrorResponse(String code, String message)`), `metrics` (sub-record `JobMetricsResponse(Long filesDiscovered, ...)`), `timestamps` (sub-record `JobTimestampsResponse(Instant createdAt, Instant startedAt, Instant updatedAt, Instant completedAt)`)). Declarar los sub-records `public static record` dentro del archivo o en archivos separados según convención del repo.
- [ ] 5.3 Crear `infrastructure/web/analyzer/AnalysisJobResponseMapper.java` con `toAccepted(AnalysisJobId, AnalysisJobStatus)` y `toStatus(AnalysisJobSnapshot)`. Incluir mapa estático `AnalysisJobPhase → String phaseLabel` alineado con los 8 stages del frontend (ver tabla del diseño).
- [ ] 5.4 Crear `infrastructure/web/analyzer/AnalysisJobController.java` (`@RestController`) con `POST /api/analyze` (delegando en `AnalysisJobSubmissionService.submit`, respuesta `ResponseEntity.accepted().body(toAccepted(...))`) y `GET /api/analyze/jobs/{jobId}` (delegando en `AnalysisJobQueryService.findById`, 200 con el snapshot mapeado o 404).
- [ ] 5.5 Modificar `infrastructure/web/analyzer/RepositoryAnalysisController.java`: eliminar `@PostMapping("/api/analyze")` y su método. Mantener `GET /api/analyze/{id}`, `GET /api/analyze/{id}/cost-breakdown`, badge, og-image, HTML público y leaderboards sin cambios.
- [ ] 5.6 Modificar `infrastructure/web/WebMvcConfiguration.java`: el `AnalyzeRateLimitInterceptor` se aplica solo a `addPathPatterns("/api/analyze")` con `excludePathPatterns("/api/analyze/jobs/**", "/api/analyze/*")` para garantizar que el GET de job y el GET de analysis nunca golpean el rate limiter.
- [ ] 5.7 Revisar `infrastructure/web/RepositoryIntakeExceptionHandler.java`: confirmar que `RATE_LIMITED → 429`, `INVALID_URL → 400`, `CLONE_TIMEOUT → 504/500` (según handler actual). Añadir mapping para `JOB_INTERRUPTED` solo si se sirve por API; en este ticket vive dentro del body, no en headers HTTP, así que no requiere cambio (verificar y dejar TODO si aplica).
- [ ] 5.8 Crear `infrastructure/web/analyzer/AnalysisJobControllerTest.java` (`@WebMvcTest(AnalysisJobController.class)`) cubriendo los 8 acceptance scenarios del spec: (a) POST válido → 202 con `jobId`/`statusUrl`/`analysisId=null`; (b) POST URL inválida → 400 `INVALID_URL` y sin row creada (verificable mockeando submission); (c) POST con executor saturado → 429 `RATE_LIMITED`; (d) GET `QUEUED` → 200 con `progress<=99`; (e) GET `SUCCESS` → 200 con `progress=100, analysisId != null`; (f) GET `FAILED` → 200 con `error.code/message` no nulos, `analysisId=null`; (g) GET id desconocido → 404; (h) GET repetido a alta cadencia no produce 429 (verificar que el interceptor no se monta para esa ruta).
- [ ] 5.9 Crear test `AnalysisJobControllerAsyncTest` (o añadir al anterior): mock de `AnalysisJobExecutionService.runJob` que bloquea 5s; el POST devuelve antes de 200 ms — garantiza que la pipeline corre fuera del hilo HTTP.
- [ ] 5.10 Migrar `infrastructure/web/analyzer/RepositoryAnalysisControllerTest.java`: eliminar los tests del antiguo POST y conservar los del GET `/api/analyze/{id}`, cost-breakdown, badge, og-image, leaderboards. Si quedan vacíos, dejar la clase con los GET existentes únicamente.

## 6. Frontend — API y hook

- [ ] 6.1 Modificar `frontend/src/types/api.ts` (o `frontend/src/types/analysisJob.ts` si se prefiere archivo dedicado): añadir `type JobStatus = 'QUEUED'|'RUNNING'|'SUCCESS'|'FAILED'`, `type JobPhase = ...`, `AnalysisJobAcceptedResponse`, `AnalysisJobStatusResponse`, `JobMetrics`, `JobError`, `JobTimestamps` espejando los DTOs del backend.
- [ ] 6.2 Modificar `frontend/src/services/api.ts` (o `frontend/src/api/analyze.ts`): cambiar `submitAnalysis(url)` para devolver `Promise<AnalysisJobAcceptedResponse>` (POST `/api/analyze`); añadir `fetchAnalysisJobStatus(jobId, signal?)` (`GET /api/analyze/jobs/{jobId}`, 404 lo propaga como `ApiError` con status 404). Mantener `getAnalysis(id)` intacto.
- [ ] 6.3 Crear `frontend/src/hooks/useAnalysisJob.ts` siguiendo el diseño: `setTimeout` encadenado (no `setInterval`), `AbortController` por request, `abort()` al desmontar o cambiar `jobId`, detiene polling cuando `status ∈ {SUCCESS, FAILED}` o tras error 404; reintenta hasta 3 veces con backoff `intervalMs*(n+1)` en errores 5xx. Devuelve `{ job, isPolling, error }`.

## 7. Frontend — rewire UI

- [ ] 7.1 Localizar `LoadingState` (probablemente en `frontend/src/pages/DashboardPage.tsx` o `frontend/src/components/LoadingState.tsx`). Confirmar la ruta antes de tocar.
- [ ] 7.2 Modificar `DashboardPage.tsx`: `handleSubmit` ahora recibe `AnalysisJobAcceptedResponse` y guarda `setActiveJobId(jobId)`. Pasar `jobId` a `LoadingState`.
- [ ] 7.3 Modificar `LoadingState`: consumir `useAnalysisJob(jobId)`. Eliminar el `setInterval` que simulaba progreso, el array `liveStats` sintético y el avance autónomo por timer.
- [ ] 7.4 Implementar el mapping `JobPhase → stageIndex` (tabla del diseño) en el componente para mantener los 8 stages visuales. `activeStage` se calcula desde `job.phase` y, dentro de `COUNTING_TOKENS`, se desplaza a 4/5 según `metrics.contextWindows`/`metrics.pricingModelsProcessed`.
- [ ] 7.5 Bindear las cards/numbers a `job.metrics` con fallback `"—"` o `0` cuando un campo es `null`.
- [ ] 7.6 Clampar la barra de progreso visual a `Math.min(99, job.progressPercent)` salvo cuando `status === 'SUCCESS' && analysisId !== null` (allí muestra 100).
- [ ] 7.7 Al recibir snapshot con `status === 'SUCCESS' && analysisId !== null`: detener polling (lo hace el hook), navegar a la vista de resultado (`window.history.pushState(null, '', analysisPath(analysisId))` + setter de ruta del Dashboard).
- [ ] 7.8 Al recibir `status === 'FAILED'`: mostrar `toUserMessage({ status: 0, code: job.error.code, message: job.error.message })` reutilizando la alerta de error existente. Limpiar `activeJobId`.

## 8. Frontend — setup de testing

- [ ] 8.1 Añadir devDeps en `frontend/package.json`: `vitest@^2`, `@testing-library/react@^16`, `@testing-library/jest-dom@^6`, `@testing-library/dom@^10`, `jsdom@^25`. Scripts `"test": "vitest run"`, `"test:watch": "vitest"`.
- [ ] 8.2 Crear `frontend/vitest.config.ts` con `environment: 'jsdom'`, `setupFiles: ['./src/test/setup.ts']`, `globals: true`. Alternativa: extender `vite.config.ts` con la sección `test` si el equipo prefiere un único config.
- [ ] 8.3 Crear `frontend/src/test/setup.ts` con `import '@testing-library/jest-dom'` y cualquier mock global (por ejemplo `vi.stubGlobal('fetch', ...)` si se considera necesario).
- [ ] 8.4 Crear `frontend/src/hooks/useAnalysisJob.test.ts` con `vi.useFakeTimers()`: (a) polling avanza `QUEUED → RUNNING → SUCCESS` y deja de pollear (mock fetch que devuelve snapshots secuenciales); (b) `FAILED` expone `error` y detiene polling; (c) `progressPercent` nunca rinde 100 al consumidor hasta que el snapshot trae `status='SUCCESS' && analysisId != null`.
- [ ] 8.5 Documentar en `frontend/README.md` (o equivalente) cómo correr `npm run test` localmente. Si no existe README de frontend, añadir un párrafo al `README.md` raíz (sección "Testing").

## 9. Docs

- [ ] 9.1 Actualizar `docs/API.md`: cambiar `POST /api/analyze` a `202 Accepted` con el nuevo body (`jobId`, `status`, `statusUrl`, `analysisId`); añadir `GET /api/analyze/jobs/{jobId}` con esquema completo de `AnalysisJobStatusResponse`; documentar `404` para jobId desconocido y que el endpoint no está sujeto al rate limiter.
- [ ] 9.2 Actualizar la sección de error codes en `docs/API.md`: añadir `JOB_INTERRUPTED`, aclarar que `CLONE_TIMEOUT`/`REPOSITORY_TOO_LARGE`/`ANALYSIS_FAILED` ahora se materializan dentro del body del job (no como HTTP 5xx).
- [ ] 9.3 Actualizar `README.md`: cualquier ejemplo `curl` o descripción de flujo que mencione la respuesta síncrona; añadir nota sobre el estado `QUEUED` (la cola admite hasta 32 jobs antes de devolver 429).
- [ ] 9.4 Documentar en `docs/ARCHITECTURE.md` (o donde corresponda) la nueva capability `analysis-jobs`, el bean `analysisJobExecutor`, el reaper y el scheduler de retención.
- [ ] 9.5 Revisar `CLAUDE.md`: solo editar si alguna regla arquitectónica cambia (p.ej. listar `domain/job` como package). Probablemente no requiere edición; dejar nota en el commit si se decide no tocar.

## 10. Smoke test manual

- [ ] 10.1 Ejecutar el plan manual: (a) `docker compose up --build -d`; (b) `POST /api/analyze` con repo pequeño (`tokenmeter` propio) y verificar `202` + `jobId`; (c) poll `GET /api/analyze/jobs/{jobId}` hasta `SUCCESS/100` y abrir la vista del resultado; (d) lanzar análisis grande (>30 s) desde la UI y confirmar que el progreso nunca llega a 100 hasta el final; (e) URL inválida → alerta `INVALID_URL` sin job creado; (f) simular fallo de clone (URL inexistente o repo privado) y comprobar `FAILED` con `error.code`/`error.message`; (g) `docker compose restart backend` mid-job y verificar que el job huérfano queda `FAILED/JOB_INTERRUPTED`. Documentar resultado en el PR.
