spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://db:5432/${TOKENMETER_DB_NAME:tokenmeter}}
    username: ${DATABASE_USERNAME:${TOKENMETER_DB_USER}}
    password: ${DATABASE_PASSWORD:${TOKENMETER_DB_PASSWORD}}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
